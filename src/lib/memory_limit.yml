apiVersion: v1
kind: Pod
spec:
  containers:
  - name: POD_NAME
    image: POD_IMAGE
    resources:
      limits:
        memory: MEMORY_LIMIT
        cpu: CPU_LIMIT
      requests:
        memory: MEMORY_REQUEST
        cpu: CPU_REQUEST
